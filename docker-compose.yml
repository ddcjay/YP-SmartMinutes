# 開發環境 Docker Compose（僅供需要時使用 PostgreSQL 的場景）
# MVP 版本使用 SQLite，無需啟動任何 Docker 容器即可運行。
# 如未來升級為團隊版需要 PostgreSQL，可取消以下註解。

# services:
#   db:
#     image: postgres:16-alpine
#     ports: ["5432:5432"]
#     environment:
#       POSTGRES_DB: smartminutes
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
#     volumes:
#       - pgdata:/var/lib/postgresql/data
#
# volumes:
#   pgdata:
